# Node-based ASAR archive unpacker

For when you don't want to install npm and pull dozens of dependencies.

This script takes the .asar file as argument and unpacks everything to the current folder.
